import DashboardSkeleton from '@/app/ui/skeletons';
export default function Loading() {
    return <DashboardSkeleton />;
}

/// this is the implementation of a streaming service for the whole page i.e. when data takes too long to come from the db